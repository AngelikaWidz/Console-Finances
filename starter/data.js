// Access array - 


// Access dates to find out how many dates there are in total - create var = totalMonths
// Using a function which adds together?
// Then print the amount of months to console/console.log()


// Acess Profit/Losses array - create var = PL (Profits/Losses access)
// Add together all Profits/Losses - create var totalPL
// Use totalMonths from previous to do totalPL/totalMonths = averageChange
// Print averageChange to console/console.log()


// Greatest profits 
// const max = Math.max(finances);
// mostPL = [0]
// if PL > mostPL: Print/console.log()


// Least Profits
// const min = Math.min(finances);
// leastPL = [0]
// if PL < leastPL: Print/console.log()


sum = finances.reduce((a,b) => a + b);
console.log(sum);


if (finances = Number);
maxProfit = Math.max(finances);
console.log(maxProfit);


var pl = finances.filter(finances => finances.profit === Number);

// if (finances = Number);
console.log([finances].reduce((a,b) => a + b));

for (var i=0; i < finances.length; i++) {
    console.log("something " + finances[1]);
    }


var profits = finances[0][1];
const sum = finances.reduce((a,b)=> a+b);
console.log(profits);